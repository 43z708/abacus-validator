import { __extends } from "tslib";
import { getSerdePlugin } from "@aws-sdk/middleware-serde";
import { Command as $Command } from "@aws-sdk/smithy-client";
import { VerifyRequest, VerifyResponse } from "../models/models_0";
import { deserializeAws_json1_1VerifyCommand, serializeAws_json1_1VerifyCommand } from "../protocols/Aws_json1_1";
var VerifyCommand = (function (_super) {
    __extends(VerifyCommand, _super);
    function VerifyCommand(input) {
        var _this = _super.call(this) || this;
        _this.input = input;
        return _this;
    }
    VerifyCommand.prototype.resolveMiddleware = function (clientStack, configuration, options) {
        this.middlewareStack.use(getSerdePlugin(configuration, this.serialize, this.deserialize));
        var stack = clientStack.concat(this.middlewareStack);
        var logger = configuration.logger;
        var clientName = "KMSClient";
        var commandName = "VerifyCommand";
        var handlerExecutionContext = {
            logger: logger,
            clientName: clientName,
            commandName: commandName,
            inputFilterSensitiveLog: VerifyRequest.filterSensitiveLog,
            outputFilterSensitiveLog: VerifyResponse.filterSensitiveLog,
        };
        var requestHandler = configuration.requestHandler;
        return stack.resolve(function (request) {
            return requestHandler.handle(request.request, options || {});
        }, handlerExecutionContext);
    };
    VerifyCommand.prototype.serialize = function (input, context) {
        return serializeAws_json1_1VerifyCommand(input, context);
    };
    VerifyCommand.prototype.deserialize = function (output, context) {
        return deserializeAws_json1_1VerifyCommand(output, context);
    };
    return VerifyCommand;
}($Command));
export { VerifyCommand };
